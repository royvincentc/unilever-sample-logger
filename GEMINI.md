# Git Commit Rule

Whenever the user asks you to "commit" changes, you MUST:
1. Stage and commit the changes to the local repository.
2. Immediately run `git push` to push the changes to GitHub.
3. Do not prompt the user for permission to push. Just make it happen automatically.
